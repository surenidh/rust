# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
name = "holiday_checker"
version = "0.1.0"
build = false
exclude = [
    "target/*",
    "tests/*",
    "examples/*",
    "docs/*",
]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "A crate for managing and querying holiday data, including filtering by date, type, and category and whether date is a holiday from a JSON file."
documentation = "https://docs.rs/holiday_checker/"
readme = "README.md"
keywords = [
    "holiday",
    "calendar",
    "2025",
    "poya",
]
categories = [
    "date-and-time",
    "command-line-utilities",
]
license = "MIT"
repository = "https://github.com/surenidh/rust"

[lib]
name = "holiday_checker"
path = "src/lib.rs"

[dependencies.chrono]
version = "0.4"
features = ["serde"]

[dependencies.serde]
version = "1.0"
features = ["derive"]

[dependencies.serde_json]
version = "1.0"
